<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="project.ppaya.square.shmapper.SH_Mapper_User">

<select id="getUserByEmail"  parameterType="String" resultType="User">
SELECT
	*
FROM
	TABLE_U
WHERE
	USER_ID = #{USER_ID}
</select>

<insert id="inputUser"  parameterType="User">
INSERT INTO
	table_u (user_id, password, name, region)
	values(#{user_id},#{password},#{name}, #{region}) 

</insert>


<!--  erd 기준으로  user_hashtag 테이블이 sql에 올라오면 살리기
<select id="getUserHashtag"  parameterType="String" resultType="UserHashtag">
SELECT
	*
FROM
	나중에 생성하는 거 보고 테이블 이름
WHERE
	USER_ID = #{USER_ID}
</select>

 -->
</mapper>
